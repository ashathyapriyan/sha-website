<section class="cat-hero" style="--accent: {{accent}}">
  <div class="cat-hero-inner">
    <span class="cat-hero-label">✍️ Blog</span>
    <p>{{description}}</p>
  </div>
</section>

<section class="section">
  {{#if posts.length}}
    <div class="card-grid">
      {{#each posts}}
        <article class="card">
          <a href="/blog/{{this.slug}}" class="card-link">
            <div class="card-image" style="background-image:url('{{this.imageUrl}}')"></div>
            <div class="card-body">
              <span class="card-badge">{{this.category}}</span>
              <h3 class="card-title">{{this.title}}</h3>
              <p class="card-excerpt">{{excerpt this.content 140}}</p>
              <div class="card-meta"><span>By {{this.author}}</span><span>{{formatDate this.createdAt}}</span></div>
            </div>
          </a>
        </article>
      {{/each}}
    </div>
  {{else}}
    <p class="empty-state">No posts yet.</p>
  {{/if}}
</section>
